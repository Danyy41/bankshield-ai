"""Phase 4: AI-assisted financial crime investigation.

Reads Phase 1-3 outputs (models, transaction/login/graph data) through
`data_access.py` and never modifies them. See the package modules for the
service layer, RAG, LLM client, tool registry, agent loop, and
human-approval gate.
"""
